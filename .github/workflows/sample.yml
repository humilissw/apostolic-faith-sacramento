name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Run-CI-Python:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    uses: humilissw/apostolic-faith-sacramento/.github/workflows/python.yml@feature/add-ci # Use main branch or specific commit SHA

  Run-CI-Node:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    uses: humilissw/apostolic-faith-sacramento/.github/workflows/node.yml@feature/add-ci # Use main branch or specific commit SHA

  # call-workflow-passing-data:
  #   permissions:
  #     contents: read
  #     pull-requests: write
  #   uses: humilissw/apostolic-faith-sacramento/.github/workflows/called-workflow.yml@feature/add-ci # Use main branch or specific commit SHA
  #   with:
  #     config-path: .github/labeler.yml
  #   secrets:
  #     token: ${{ secrets.GITHUB_TOKEN }}
  # call-workflow-passing-data:
  #   permissions:
  #     contents: read
  #     pull-requests: write
  #   uses: octo-org/example-repo/.github/workflows/workflow-B.yml@main
  #   with:
  #     config-path: .github/labeler.yml
  #   secrets:
  #     token: ${{ secrets.GITHUB_TOKEN }}

    # with:
    #   message: "Hello from the caller!"
    # secrets: inherit # Optional: pass all secrets automatically

  # use-output:
  #   needs: [Run-CI-Python]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Get time output
  #       run: echo "The called workflow finished at: ${{ needs.Run-CI-Python.outputs.current_time }}"

  # Run-CI-Node:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: node.yml


# # .github/workflows/calling-workflow.yml
# name: Calling Workflow
# on: [push]

# jobs:
#   call-workflow:
#     uses: MyOrganization/MyRepository/.github/workflows/called-workflow.yml@main # Use main branch or specific commit SHA
#     with:
#       message: "Hello from the caller!"
#     secrets: inherit # Optional: pass all secrets automatically

#   use-output:
#     needs: [call-workflow]
#     runs-on: ubuntu-latest
#     steps:
#       - name: Get time output
#         run: echo "The called workflow finished at: ${{ needs.call-workflow.outputs.current_time }}"

  
