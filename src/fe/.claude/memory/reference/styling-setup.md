---
name: Styling Setup
description: Tailwind, HeroUI, and component library configuration
type: reference
---

Tailwind CSS v4 with `@tailwindcss/postcss`. HeroUI (`@heroui/react`) for base components. Radix UI primitives for headless UI. Custom Tailwind config in `tailwind.config` if it exists.
