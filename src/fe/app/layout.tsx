import type { Metadata } from "next";
import "./globals.css";
import { Toaster } from "sonner";

export const metadata: Metadata = {
  title: "Apostolic Faith Church Sacramento",
  description: "Generated by create next app",
  other: {
    "content-security-policy": [
      "default-src 'self';",
      "script-src 'self' https://*.stripe.com https://*.stripe.network; ",
      "style-src 'self' https://*.stripe.com; ",
      "img-src 'self' data:;",
      "font-src 'self';",
      "connect-src 'self';",
      "media-src https://*.youtube.com https://*.youtu.be https://*.vimeo.com https://*.player.vimeo.com;",
      "frame-src https://www.google.com/maps https://*.youtube.com https://*.player.vimeo.com https://*.vimeo.com;",
      "frame-ancestors https://*.youtube.com https://*.vimeo.com;",
      "worker-src 'self' blob:;",
      "object-src 'none';",
      "base-uri 'self';",
      "form-action 'self' https://*.stripe.com;",
      "upgrade-insecure-requests",
    ].join(" "),
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" >
      <body className="flex flex-col min-h-dvh">
        {children}
        <Toaster position="top-right" />
      </body>
    </html>
  )
}
